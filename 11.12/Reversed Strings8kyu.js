function solution(str){
    return str.split('').reverse().join(''); //сначало методоб split преобразуем str в массив, после reverse переворачивает массив, потом join добавляет обратно уже перевернутый
}